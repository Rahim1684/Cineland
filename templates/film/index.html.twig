{% extends 'base.html.twig' %}

{% block title %}Film index{% endblock %}

{% block body %}
    <h1>Film index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Duree</th>
                <th>DateSortie</th>
                <th>Note</th>
                <th>AgeMinimal</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        <a href="{{ path('film_new') }}">Ajouter</a> <br>
            
        {% for film in films %}
            <tr>
                <td>{{ film.id }}</td>
                <td>{{ film.titre }}</td>
                <td>{{ film.duree }}</td>
                <td>{{ film.dateSortie ? film.dateSortie|date('Y-m-d') : '' }}</td>
                <td>{{ film.note }}</td>
                <td>{{ film.ageMinimal }}</td>
                <td>
                    <a href="{{ path('film_show', {'id': film.id}) }}">Détails</a>
                    <a href="{{ path('film_edit', {'id': film.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
